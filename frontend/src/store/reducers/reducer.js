// import {} from "../actions/action";

const initialState = {
  test: 's'
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
  return state;
};

export default reducer;
